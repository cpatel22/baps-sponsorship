# Database Idle State Handling - Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          User Browser                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                      Any Page (e.g., /home, /admin)                   │  │
│  ├──────────────────────────────────────────────────────────────────────┤  │
│  │                                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────────┐    │  │
│  │  │  DBStatusChecker Component (Client Component)               │    │  │
│  │  │  - Mounts on every page                                     │    │  │
│  │  │  - Checks DB health via API                                 │    │  │
│  │  │  - Shows overlay if DB is idle                              │    │  │
│  │  │  - Attempts auto-recovery                                   │    │  │
│  │  └─────────────────────────────────────────────────────────────┘    │  │
│  │                              │                                        │  │
│  │                              │ fetch()                                │  │
│  └──────────────────────────────┼────────────────────────────────────────┘  │
│                                 │                                           │
└─────────────────────────────────┼───────────────────────────────────────────┘
                                  │
                                  │ HTTP Request
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Next.js Server                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  Middleware (src/middleware.ts)                                       │  │
│  │  - Runs on every request                                             │  │
│  │  - Can add pre-warming logic                                         │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  API Routes                                                           │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │  GET /api/db-health                                            │  │  │
│  │  │  - Checks database connection                                  │  │  │
│  │  │  - Returns health status JSON                                  │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │  POST /api/db-health/wake-up                                   │  │  │
│  │  │  - Attempts to wake up database                                │  │  │
│  │  │  - Retries up to 5 times                                       │  │  │
│  │  │  - Returns success/failure status                              │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                 │                                           │
│                                 │ calls                                     │
│                                 ▼                                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  Database Health Module (src/lib/db-health.ts)                        │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │  checkDatabaseHealth()                                         │  │  │
│  │  │  - Executes test query: SELECT 1                               │  │  │
│  │  │  - Measures response time                                      │  │  │
│  │  │  - Detects idle state from errors                              │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │  wakeUpDatabase()                                              │  │  │
│  │  │  - Retry loop with delays                                      │  │  │
│  │  │  - Calls checkDatabaseHealth() repeatedly                      │  │  │
│  │  │  - Returns final status                                        │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                 │                                           │
│                                 │ uses                                      │
│                                 ▼                                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  Database Connection Module (src/lib/db-azure.ts)                     │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │  getPool()                                                     │  │  │
│  │  │  - Creates/returns connection pool                            │  │  │
│  │  │  - Enhanced error handling                                    │  │  │
│  │  │  - Prevents concurrent connection attempts                    │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │  Database Class                                                │  │  │
│  │  │  - getEvents(), getUsers(), etc.                              │  │  │
│  │  │  - All CRUD operations                                        │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                 │                                           │
└─────────────────────────────────┼───────────────────────────────────────────┘
                                  │ SQL Connection
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       Azure SQL Database                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  States:                                                                    │
│  - Active: Responds to queries                                             │
│  - Idle/Sleeping: Needs time to wake up (10-30 seconds)                    │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Flow Diagrams

### Happy Path (DB is Active)

```
User → Page Load → DBStatusChecker
                         │
                         ▼
                   GET /api/db-health
                         │
                         ▼
                   checkDatabaseHealth()
                         │
                         ▼
                   Query Database
                         │
                         ▼
                   ✅ Success (50ms)
                         │
                         ▼
                   Return { isHealthy: true }
                         │
                         ▼
                   Component shows nothing
                         │
                         ▼
                   User sees normal page
```

### Idle Path (DB is Sleeping)

```
User → Page Load → DBStatusChecker
                         │
                         ▼
                   GET /api/db-health
                         │
                         ▼
                   checkDatabaseHealth()
                         │
                         ▼
                   Query Database
                         │
                         ▼
                   ❌ Timeout Error
                         │
                         ▼
                   Return { isHealthy: false, isIdle: true }
                         │
                         ▼
                   Component shows overlay
                         │
                         ▼
                   POST /api/db-health/wake-up
                         │
                         ▼
                   wakeUpDatabase()
                         │
         ┌───────────────┴───────────────┐
         ▼                               ▼
    Attempt 1 ────────────────────► Attempt 2
    (fail, wait 3s)                 (fail, wait 3s)
         │                               │
         ▼                               ▼
    Attempt 3 ────────────────────► Attempt 4
    (fail, wait 3s)                 (success! 🎉)
                                         │
                                         ▼
                               Return { isHealthy: true }
                                         │
                                         ▼
                               Update overlay: "Ready!"
                                         │
                                         ▼
                               Auto-refresh page (1s)
                                         │
                                         ▼
                               User sees normal page
```

### Failure Path (DB Cannot Wake)

```
User → Page Load → DBStatusChecker
                         │
                         ▼
                   POST /api/db-health/wake-up
                         │
                         ▼
                   wakeUpDatabase()
                         │
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
    Attempt 1       Attempt 2       Attempt 3
      (fail)          (fail)          (fail)
         │               │               │
         └───────────────┴───────────────┘
                         │
                         ▼
                   All 5 attempts failed
                         │
                         ▼
             Return { isHealthy: false, isIdle: true }
                         │
                         ▼
             Show overlay with "Retry" button
                         │
                         ▼
             User clicks retry manually
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│  layout.tsx                                                  │
│  ├── NavBar                                                  │
│  ├── DBStatusChecker ◄──── YOU ARE HERE                      │
│  │   └── Overlay (conditional)                               │
│  │       ├── Loading State                                   │
│  │       └── Error State with Retry Button                   │
│  └── children (page content)                                 │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

### Health Check Response

```typescript
{
  isHealthy: boolean,      // Can execute queries?
  isIdle: boolean,         // Is database sleeping?
  message: string,         // User-friendly message
  lastChecked: Date,       // When was this checked
  responseTime?: number    // How long did query take (ms)
}
```

### States

```
┌──────────────────────────────────────────────────────────┐
│  DB Status States                                         │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  1. Healthy & Active                                      │
│     ✅ isHealthy: true                                    │
│     ✅ isIdle: false                                      │
│     → Show nothing to user                                │
│                                                           │
│  2. Idle/Sleeping                                         │
│     ❌ isHealthy: false                                   │
│     ⏸️  isIdle: true                                       │
│     → Show loading overlay + auto wake-up                 │
│                                                           │
│  3. Error (Non-idle)                                      │
│     ❌ isHealthy: false                                   │
│     ❌ isIdle: false                                      │
│     → Show error overlay + retry button                   │
│                                                           │
│  4. Waking Up                                             │
│     ⏳ isHealthy: false                                   │
│     🔄 isIdle: true                                       │
│     🔧 isChecking: true                                   │
│     → Show "waking up" message                            │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## File Dependencies

```
layout.tsx
    │
    └─► DBStatusChecker.tsx (client)
            │
            ├─► /api/db-health/route.ts
            │       │
            │       └─► db-health.ts
            │               │
            │               └─► db-azure.ts → Azure SQL
            │
            └─► /api/db-health/wake-up/route.ts
                    │
                    └─► db-health.ts
                            │
                            └─► db-azure.ts → Azure SQL
```

## Optional: Server Action Wrapping

```
actions.ts (Server Actions)
    │
    ├─► withDBAction() ◄── db-action-wrapper.ts
    │       │
    │       └─► withDBConnection() ◄── db-wrapper.ts
    │               │
    │               └─► db-azure.ts → Azure SQL
    │
    └─► safeDBQuery() ◄── db-action-wrapper.ts
            │
            └─► withDBConnection() ◄── db-wrapper.ts
                    │
                    └─► db-azure.ts → Azure SQL
```

## Key Design Decisions

### 1. **Client-Side Detection**
- ✅ Runs in browser (doesn't block server)
- ✅ Can show UI feedback immediately
- ✅ Doesn't impact server-side rendering

### 2. **Automatic Recovery**
- ✅ User doesn't need to refresh manually
- ✅ Retry logic handles transient issues
- ✅ Configurable retry count and delays

### 3. **Progressive Enhancement**
- ✅ Works without JavaScript (server still handles errors)
- ✅ Enhanced experience with JavaScript
- ✅ Graceful degradation

### 4. **Separation of Concerns**
- ✅ UI logic in component
- ✅ Health check logic in dedicated module
- ✅ DB operations in db layer
- ✅ API routes for HTTP interface

## Performance Considerations

```
┌──────────────────────────────────────────────────────────┐
│  Performance Impact                                       │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  On Page Load (DB Healthy):                              │
│    - Health check: ~50ms                                  │
│    - HTTP roundtrip: ~20ms                                │
│    - Total delay: ~70ms (barely noticeable)               │
│                                                           │
│  On Page Load (DB Idle):                                  │
│    - Initial check: ~30s (timeout)                        │
│    - Wake-up attempts: 5 × 3s = 15s                       │
│    - Total: ~45s (with progress shown to user)            │
│                                                           │
│  Bundle Size Impact:                                      │
│    - DBStatusChecker: ~2KB                                │
│    - Dependencies: 0 (uses built-in fetch)                │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

---

This architecture ensures:
- ✅ **Reliability**: Automatic detection and recovery
- ✅ **User Experience**: Clear feedback and status
- ✅ **Performance**: Minimal impact when DB is healthy
- ✅ **Maintainability**: Clean separation of concerns
- ✅ **Extensibility**: Easy to add monitoring, logging, etc.
